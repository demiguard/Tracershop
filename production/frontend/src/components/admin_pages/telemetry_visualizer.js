import React from "react";

export function TelemetryVisualizer(){
  const [telemetryRecords, setTelemetryRecords] = useState(new Map())
  const [telemetryRequest, setTelemetryRequests] = useState(new Map())


  return (
    <div>

    </div>
  );
}