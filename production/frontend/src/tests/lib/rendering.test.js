import React from "react";
import { render, unmountComponentAtNode } from "react-dom";
import { act } from "react-dom/test-utils";

import { renderStatusImage, renderTableRow, renderDateTime, renderSelect, renderComment, renderClickableIcon, renderOnClose } from "../../lib/rendering.js";

test("render OnClose, Test", () => {
  const onCloseFunction = () => {
    
  };
});
